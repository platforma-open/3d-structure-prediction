---
"@platforma-open/milaboratories.3d-structure-prediction.ui": minor
---

Open a 3D structure preview on row click. Adds a slide-modal that renders the predicted PDB via `<PlStructureViewer>` from `@milaboratories/structure-viewer`, with a single-PDB download inside the modal. Bulk export keeps its existing toolbar button.
